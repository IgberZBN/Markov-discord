interface ConfigParam {
  wordsCount: number;
  sampleSize: number;
  delay: number;
}

const configParam: ConfigParam = {
  wordsCount: 200,
  sampleSize: 3,
  delay: 10 * 60000,
};

export default configParam;
